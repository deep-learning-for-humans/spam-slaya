version: '3.8'

services:
  webapp:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    command: python run.py

  worker:
    build: .
    command: python worker.py

  redis:
    image: redis:latest
    ports:
      - "6379:6379"